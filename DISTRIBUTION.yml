# DISTRIBUTION.yml — generated by tools/schema_dist.py; do not hand-edit.
#
# The vendored surface of this package: the files downstream repositories
# copy in verbatim, and what each one must hash to. Regenerate with
# `python3 tools/schema_dist.py check . --fix`; CI fails when this file and
# the tree disagree, so the interface cannot drift silently.
#
# parity: strict  the consumer's copy must be byte-identical (sha256)
#         text    the copy may be reflowed/requoted but not changed in
#                 substance (sha256_text)
# sha256_text = sha256 of the text with ALL whitespace removed and ASCII
#               apostrophes mapped to double quotes — layout-blind, so
#               only a real edit moves it. Content-addressed only: this
#               file carries no date and no commit, so it goes stale when
#               the payload changes, never merely because time passed.
schema: distribution/v1
package: pyramid-schema
spec: "0.1"
payload:
  - path: protocol/CLAUDE.snippet.md
    parity: text
    bytes: 2187
    sha256: e2cdc8329ea5c550f4644328eb36ed28201e55776497de1f2b3ed7dc6ba63118
    sha256_text: 6c5cfdc3114a59821231b7000785a247f4ddad8ddba93c9e3ab8710f3e05131f
    purpose: the protocol block pasted into an adopter's CLAUDE.md
  - path: templates/SCHEMA.template.md
    parity: text
    bytes: 990
    sha256: a09d2e8c3ffb3e539ebb51d69b87495ad3fd10278cb260a59dce1198af29c410
    sha256_text: f0b620ac8493d6ac56bc13ac19a02205cdabd811043a7e39a86aa4358969f00f
    purpose: the seed template every new directory's SCHEMA.md is filled in from
  - path: tools/schema_lint.py
    parity: strict
    bytes: 23151
    sha256: c63c47cc737ca3d5712f48c405cb1816bb13382efac7600f347e71c67abf628f
    sha256_text: 22789f242e43c7c2e852d3f807060ef1d495372762e91cdbda2bf3f496623415
    purpose: the validator adopters run in CI; a drifted copy is a different gate

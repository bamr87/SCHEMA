---
schema: "0.1"
coverage: strict
---

# SCHEMA — templates

> Seed files copied into repositories that adopt the paradigm.

## Structure

| entry | kind | purpose | rules |
|---|---|---|---|
| `SCHEMA.template.md` | file | The canonical template for a new directory's SCHEMA.md | required |

## Forbidden

- Templates contain `{{placeholders}}` only — never repo-specific content.
